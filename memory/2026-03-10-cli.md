# 2026-03-10: fetch-podcast CLI Utility

## Summary
- Built `src/fetch_podcast/` Python CLI package with Click entry point
- Modules: `cli.py` (pipeline orchestration), `env.py` (SPOTIPY→SPOTIFY remapping + SIP workaround), `spotify.py` (subprocess wrapper), `csv_writer.py` (JSON→CSV with show_name backfill)
- 31 tests across 4 test files + conftest.py — all passing
- Registered as `uv run fetch-podcast` via pyproject.toml script entry point

## Key Decisions
- Subprocess isolation: spotify-client invoked via `uv run spotify ...` subprocess, not library import
- show_name backfill: `show episodes` endpoint returns empty show_name; injected from search result at CSV write time
- UV_CACHE_DIR=/tmp/uv-cache baked into env.py for macOS SIP workaround
- Click over argparse for consistency with spotify-client and CliRunner testing

## New Files
- `pyproject.toml` — project config with click, python-dotenv deps
- `src/fetch_podcast/__init__.py`, `cli.py`, `env.py`, `spotify.py`, `csv_writer.py`
- `tests/conftest.py`, `test_env.py`, `test_spotify.py`, `test_csv_writer.py`, `test_cli.py`
