# 2026-03-09: Initial Data Fetch

## Summary
- Fetched all 2,651 Joe Rogan Experience episodes from Spotify using `spotify-client` CLI
- Converted JSON output to `joe_rogan_episodes.csv` with 11 columns
- Show ID: `4rOoJ6Egrf8K2IrywzwOMk`

## Key Learnings
- The spotify-client `.env` uses `SPOTIPY_*` var names but code expects `SPOTIFY_*` — need to map when calling from outside
- Global CLI flags (`-c`, `-l`) must come before the subcommand
- Fetching ~2651 episodes required ~53 paginated API calls (50 per page), completed successfully with `-l 5000`
