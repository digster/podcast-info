# 2026-03-13: Use Show ID in Default CSV Filename

## Changes

- **`src/fetch_podcast/cli.py`**: Changed default output filename from `{slugified_show_name}_episodes.csv` to `{show_id}_episodes.csv`. Removed the `_slugify` helper function and the unused `import re`.
- **`tests/test_cli.py`**: Removed `_slugify` import and `TestSlugify` class (4 tests). Updated `test_default_output_filename` assertion from `test_podcast_episodes.csv` to `show1_episodes.csv`.
- **`README.md`**: Updated `--output` default description in the CLI options table.
- **`PROMPT.md`**: Appended the prompt for this change.

## Rationale

Show IDs are unique and immutable — show names can change and may collide when slugified. Using the ID produces a stable, guaranteed-unique default filename.

## Test Results

All 27 tests pass.
